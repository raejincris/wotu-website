name = "wotu-cms-auth"
main = "src/index.js"
compatibility_date = "2026-01-01"

# Allowed domains: chỉ cho admin của site WOTU dùng auth proxy này.
[vars]
ALLOWED_DOMAINS = "www.wotu.vn,wotu.vn,wotu-website.raejin-cris.workers.dev"

# Secrets (KHÔNG để trong file này, set qua dashboard / CLI):
#   GITHUB_CLIENT_ID
#   GITHUB_CLIENT_SECRET
